= title(t(".title"))
= render_modal(title: t(".title"), modal_id: "edit-product-category") do
  = turbo_frame_tag :edit_product_category_form_frame, target: :_top do
    = safe_render.partial("product_categories/form")
