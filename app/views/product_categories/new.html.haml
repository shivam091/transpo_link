= title(t(".title"))
= render_modal(title: t(".title"), modal_id: "new-product-category") do
  = turbo_frame_tag :new_product_category_form_frame, target: :_top do
    = safe_render.partial("product_categories/form")
