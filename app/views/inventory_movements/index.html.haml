.table-wrapper
  .table-container
    .table-header
      .table-cell.cell-narrow #
      - InventoryMovement::LISTING_ATTRIBUTES.each do |attribute|
        .table-cell
          = InventoryMovement.human_attribute_name(attribute)
    .table-body
      - if @inventory_movements&.each.with_index(1) do |inventory_movement, index|
        = safe_render.partial("inventory_movements/inventory_movement", inventory_movement:, index:)
      - end.empty?
        = safe_render.partial("shared/no_records", resource: InventoryMovement)
