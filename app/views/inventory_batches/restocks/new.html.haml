- title = t(".title", batch_number: @inventory_batch.batch_number)
= title(title)
= render_modal(title: title, modal_id: "inventory-batch-restock", size: :lg) do
  = turbo_frame_tag :inventory_batch_restock_form_frame, target: "_top" do
    = safe_render.partial("inventory_batches/restocks/form")
