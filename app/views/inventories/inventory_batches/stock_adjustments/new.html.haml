= title(t(".title"))
= render_modal(title: t(".title"), modal_id: "new-stock-adjustment") do
  = turbo_frame_tag :new_stock_adjustment_form_frame, target: "_top" do
    = safe_render.partial("inventories/inventory_batches/stock_adjustments/form")
