= title(t(".title", reference_code: @inventory.reference_code))
.card.card-primary.border.border-primary
  .card-body
    = turbo_frame_tag :edit_inventory_form_frame, target: :_top do
      = safe_render.partial("inventories/form")
