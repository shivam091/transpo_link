= title(t(".title", reference_code: @product.reference_code))
.card.card-primary.border.border-primary
  .card-body
    = turbo_frame_tag :edit_product_form_frame, target: :_top do
      = safe_render.partial("products/form")
