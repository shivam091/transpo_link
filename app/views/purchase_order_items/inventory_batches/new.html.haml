= title(t(".title"))
= render_modal(title: t(".title"), modal_id: "new-inventory-batch", size: :lg) do
  = turbo_frame_tag :new_inventory_batch_form_frame, target: "_top" do
    = safe_render.partial("purchase_order_items/inventory_batches/form")
