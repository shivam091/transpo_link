= title(t(".title"))
= render_modal(title: t(".title"), modal_id: "edit-purchase-order-item") do
  = turbo_frame_tag :edit_purchase_order_item_form_frame, target: "_top" do
    = safe_render.partial("purchase_order_items/form/modal_view")
