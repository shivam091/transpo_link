- title = t(".title", reference_code: @purchase_order.reference_code)
= title(title)
= render_modal(title: title, modal_id: "purchase-order-rejection") do
  = turbo_frame_tag :po_rejection_confirmation_form_frame, target: "_top" do
    = safe_render.partial("purchase_orders/rejections/form")
