= title(t(".title"))
.card.card-primary.border.border-primary
  .card-body
    .widget
      .widget-header.d-flex.align-items-center.justify-content-between
        .d-block
          .widget-title
            = t(".your_legal_identifiers")
          .widget-help
            = t(".your_legal_identifiers_help_text")
        = link_to new_legal_identifier_path, class: "btn btn-primary", data: {turbo_frame: :remote_modal} do
          = sprite_icon("plus")
          = button_text(:new)
      .widget-body
        = safe_render.partial("legal_identifiers/help_texts")
        %hr
        = safe_render.partial("legal_identifiers/nav_tabs")
        .table-responsive
          .table-container
            .table-header
              .table-cell.cell-narrow #
              - LegalIdentifier::LISTING_ATTRIBUTES.each do |attribute|
                .table-cell
                  = LegalIdentifier.human_attribute_name(attribute)
              .table-cell.cell-narrow
                = t("shared.actions")
            .table-body
              - if @legal_identifiers&.each.with_index(1) do |legal_identifier, index|
                = safe_render.partial("legal_identifiers/legal_identifier", legal_identifier:, index:)
              - end.empty?
                = safe_render.partial("shared/no_records", resource: LegalIdentifier)
      - if @pagination_metadata.total_pages > 1
        .widget-footer
          = render_pagination(@pagination_metadata)
