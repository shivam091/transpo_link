= title(t(".title"))
= render_modal(title: t(".title"), modal_id: "edit-legal-identifier") do
  = turbo_frame_tag :edit_legal_identifier_form_frame, target: "_top" do
    = safe_render.partial("legal_identifiers/form")
